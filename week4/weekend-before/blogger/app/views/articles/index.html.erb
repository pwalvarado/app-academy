<h1>All Articles</h1>

<ul id="articles">
  <% @articles.each do |article| %>
  	<li>
  		<%= link_to article.title, article_url(article), class: 'article_title' %>
  	</li>
  <% end %>
</ul>

<%= link_to 'Create a New Article', new_article_url %>